name: Build and Deploy
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Install and Build 🔧
      working-directory: /home/runner/work/portfolio/
      run: |
        npm install
        npm run build

      - name: Deploy
        uses: actions/checkout@v2
        with:
          publish_dir: ./dist/my-portfolio